apiVersion: v1
kind: Service
metadata:
  name: {{ include "business-gateway.fullname" . }}
  labels:
    {{- include "business-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "business-gateway.selectorLabels" . | nindent 4 }}
